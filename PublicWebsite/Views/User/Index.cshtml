@model DAL.Dto.EditProfileDto
@inject Microsoft.AspNetCore.Mvc.Localization.IViewLocalizer localizer
<section class="ftco-section contact-section bg-light">
    <div class="container">
        <div class="row block-9">
            <div class="col-md-6 order-md-last d-flex mx-auto">

                <form asp-controller="User" asp-action="Index" enctype="multipart/form-data" class="bg-white p-5 contact-form">
                    @*@if (ViewBag["Message"] != null && ViewBag["Message"] != "")
        {
        <div class="form-group">
            <div class="alert alert-danger" role="alert">
                @ViewBag["Message"]
            </div>
        </div>
        }*@
                   <img src="../@Model.Avatar" class="personal-img"  />
                    <div class="form-group">
                        <input type="hidden" class="form-control" value="@Model.Id"  placeholder="Firstname" name="Id">
                    </div>
                    <div class="form-group">
                        <input type="text" class="form-control"  value="@Model.Firstname" placeholder="@localizer["Firstname"]" name="Firstname">
                    </div>
                    <div class="form-group">
                        <input type="text" class="form-control"  value="@Model.Lastname" placeholder="@localizer["Lastname"]" name="Lastname">
                    </div>
                    <div class="form-group">
                        <input type="text" class="form-control"  value="@Model.Phonenumber" placeholder="@localizer["Phonenumber"]" name="Phonenumber">
                    </div>
                    <div class="form-group">
                        <input type="file" class="form-control-file"  name="Avatar">
                    </div>
                    <div class="form-group">
                        <input type="submit" value="@localizer["Update"]" class="btn btn-primary py-3 px-5  float-right">
                    </div>
                </form>

            </div>
        </div>
    </div>
</section>

